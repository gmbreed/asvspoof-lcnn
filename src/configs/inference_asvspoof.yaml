defaults:
  - model: lcnn
  - metrics: asvspoof
  - datasets: asvspoof_eval
  - dataloader: asvspoof
  - transforms: asvspoof
  - _self_

dataset_root: /kaggle/input/asvspoof2019/LA

inferencer:
  device_tensors: ["data_object", "labels"]
  device: auto
  save_path: "asvspoof_eval"
  seed: 1
  from_pretrained: saved/lcnn_baseline/model_best.pth